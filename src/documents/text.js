const STOP_WORDS = new Set(['а','без','бы','в','вам','вас','вы','где','для','до','его','ее','ещё','же','за','и','из','или','к','как','какая','какие','какой','ли','мне','можно','на','надо','не','но','о','об','он','она','они','от','по','под','про','с','со','так','такое','у','что','это','the','a','an','and','or','of','to','for','with','is','are']);
export function normalize(value){return String(value??'').toLocaleLowerCase('ru-RU').normalize('NFKD').replace(/[\u0300-\u036f]/g,'').replace(/ё/g,'е').replace(/[^\p{L}\p{N}\s-]/gu,' ').replace(/\s+/g,' ').trim();}
function stemLoose(token){if(token.length<=4)return token;return token.replace(/(иями|ями|ами|ого|ему|ими|ыми|ая|яя|ое|ее|ие|ые|ый|ий|ой|ам|ям|ах|ях|ов|ев|ом|ем|у|ю|а|я|ы|и|е)$/u,'');}
export function tokenize(value){return normalize(value).split(' ').map(stemLoose).filter(t=>t.length>1&&!STOP_WORDS.has(t));}
export function scoreText(query,text,title=''){const qn=normalize(query);const qt=[...new Set(tokenize(query))];if(!qt.length)return 0;const ht=new Set(tokenize(`${title} ${text}`));let matches=0;for(const t of qt)if(ht.has(t))matches++;const coverage=matches/qt.length;const density=matches/Math.max(4,Math.sqrt(ht.size));const phrase=qn.length>=5&&normalize(`${title} ${text}`).includes(qn)?0.35:0;const titleBoost=normalize(title).includes(qn)?0.2:0;return Math.min(1,coverage*.68+density*.12+phrase+titleBoost);}
