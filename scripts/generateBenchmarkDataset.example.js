import { BenchmarkDatasetService } from "../src/benchmark/BenchmarkDatasetService.js";

// Подключите реальные зависимости проекта:
// generator, validator, repository, knowledgeSearch.
//
// Затем:
//
// const service = new BenchmarkDatasetService({...});
// const dataset = await service.generate({
//   documentId: process.argv[2],
//   positiveCount: 50,
//   negativeCount: 50
// });
// console.log(JSON.stringify(dataset.stats, null, 2));
